import { PageLoadingCard } from "@/components/page-loading-card";

export default function Loading() {
  return (
    <PageLoadingCard
      body="Preparing your restaurant dashboard."
      eyebrow="Owner"
      title="Opening owner dashboard"
    />
  );
}
