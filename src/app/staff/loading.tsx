import { PageLoadingCard } from "@/components/page-loading-card";

export default function Loading() {
  return (
    <PageLoadingCard
      body="Preparing live kitchen and order updates."
      eyebrow="Staff"
      title="Opening staff dashboard"
    />
  );
}
