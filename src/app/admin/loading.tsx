import { PageLoadingCard } from "@/components/page-loading-card";

export default function Loading() {
  return (
    <PageLoadingCard
      body="Preparing your super admin dashboard."
      eyebrow="Admin"
      title="Opening control center"
    />
  );
}
